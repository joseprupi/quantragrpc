include "term_structure.fbs";
include "enums.fbs";

namespace quantra;

table Yield{
    day_counter:enums.DayCounter;
    compounding:enums.Compounding;
    frequency:enums.Frequency;
}

table Pricing{
    as_of_date: string;
    curves:[TermStructure];
    bond_pricing_details:bool;
    bond_pricing_flows:bool;
}